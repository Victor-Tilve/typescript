const userName = 'Victor Tilve';

console.log(userName)

//tsc app.ts -w  for using watch mode and refresh the server without compile everytime there is a change
// we create a project using the comand tsc --init and now we can compile multiples files